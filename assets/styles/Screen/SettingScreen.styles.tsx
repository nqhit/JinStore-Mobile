import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  logoutButton: {
    padding: 10,
    borderWidth: 1,
    borderColor: '#ddd',
    margin: 10,
    width: 100,
    borderRadius: 8,
    alignItems: 'center',
    backgroundColor: '#ccc',
  },
});

export default styles;
