import { ThemedText } from '@/components/ThemedText';
import { memo } from 'react';

function CartDetails() {
  return <ThemedText type="title">CartDetails</ThemedText>;
}

export default memo(CartDetails);
